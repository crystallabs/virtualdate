/docs/
/lib/
/bin/
/.shards/

# Libraries don't need dependency lock
# Dependencies will be locked in application that uses them
/shard.lock

# Local-only dependency overrides (e.g. pointing virtualtime at a sibling checkout)
/shard.override.yml

# The example app installs its own dependencies locally
/example/lib/
/example/shard.lock
